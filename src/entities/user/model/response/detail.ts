export interface UserDetailResponse {
  id:               string;
  name:             string;
  email:            string;
  profileImageUrl?: string;
  createdAt:        Date;
  updatedAt:        Date;
}
